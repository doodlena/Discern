{
  "name": "@discern/extension",
  "version": "1.0.0",
  "description": "Browser extension for DISCERN",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
